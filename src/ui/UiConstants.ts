import { Position } from "../../electron/types/Position.type";

export const ICON_SIZE_MEDIUM = 24;

export const POSITION_BOTTOM_RIGHT : Position = {
    bottom: 0,
    right: 0,
    transform: "translateY(100%)"
}

export const PROMPT_ROWS = 8;

export const PROMPT_COLS = 100;
